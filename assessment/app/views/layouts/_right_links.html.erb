<% if user_signed_in? %>
  <p class="text-primary">Logged in as <%= current_user.email %></o><br>
  <p><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></p>
  <p><%= link_to "Edit Your Profile", edit_user_path(current_user) %></p>
<% end %>