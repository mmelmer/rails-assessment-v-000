<h1><center><strong>Browse Reviews by User.</strong></center></h1>
<hr>
<center><div class="container">
  <% @users.each do |user| %>
    <p class="btn"><%= link_to "#{user.email}", user_path(user) %></p>
  <% end %>
</div></center>